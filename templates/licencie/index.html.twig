{% extends 'base.html.twig' %}
{% block title %}Liste des Licencie{% endblock %}
{% block body %}
   
      <!-- Table -->
 <div class="row mt-5">
        <div class="col">
          <div class="card bg-default shadow">
            <div class="card-header bg-transparent border-0 d-flex justify-content-between align-items-center">
              <h3 class="text-white mb-0">Licencies</h3>
               <a href="{{ path('addLicencie') }}" class="btn btn-warning"><i class="fas fa-plus-circle "></i> </a>
            </div>

           
            <div class="table-responsive">
              <table class="table align-items-center table-dark table-flush">
                <thead class="thead-dark">
                  <tr>
                    
                    <th scope="col">Nom</th>
                    <th scope="col">Prénom</th>
                    <th scope="col">Numero de licence</th>
                    <th scope="col">Contact</th>
                    <th scope="col">Catégorie</th>
                   
                    <th scope="col">Actions</th>
                  </tr>
                </thead>
                <tbody>
                 {% for licencie in licencies %}
                  <tr>
                    <th scope="row">
                      <div class="media align-items-center">
                        <a href="#" class="avatar rounded-circle mr-3">
                          <img alt="Image placeholder" src="{{ asset('assets/img/theme/bootstrap.jpg') }}">
                        </a>
                        <div class="media-body">
                          <span class="mb-0 text-sm"> {{ licence.Nom }}</span>
                        </div>
                      </div>
                    </th>
                    <td>
                      <span class="badge badge-dot mr-4">
                        <i class="bg-warning"></i> {{ licence.Prenom }}
                      </span>
                    </td>
                   <td>
                      <span class="badge badge-dot mr-4">
                        <i class="bg-warning"></i> {{ licence.numLicence }}
                      </span>
                    </td>
                    <td>
                      <div class="d-flex align-items-center">
                 
                      
                      </div>
                    </td>
                    <td class="actions"> 
                        <div class="">
                        <a href="{{ path('editEducateur', {'id': licence.id}) }}" class="dropdown-item mr-1 "><i class="fas fa-edit"></i> </a>
                        <a href="{{ path('deleteEducateur', {'id': licence.id}) }}" class="dropdown-item"><i class="fas fa-trash-alt"></i> </a>   
                        </div>
                    </td>
                  </tr>
           
               {% endfor %}
                
                </tbody>
              </table>
            </div>
          </div>
        </div>
      </div>
    </div>

{% endblock %}

 
 
 
 
 
 
 
 
 
 
 
 